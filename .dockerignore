# Dependencias (se instalan con npm ci dentro de la imagen)
server/node_modules
app/node_modules

# Datos en runtime (SQLite, uploads, WAL)
server/data
data
**/*.db
**/*.db-shm
**/*.db-wal

# Entorno local: nunca se copia al contenedor
**/.env

# Fuente del frontend: solo se empaqueta app/dist compilado
app/src
app/public
app/scripts
app/index.html
app/*.json
app/*.js
app/*.ts

# Tests y herramientas
server/tests
e2e-screenshots
verification
taskdeck-mockup

# VCS / SO
.git
.gitignore
**/.DS_Store
*.log
Dockerfile
.dockerignore
